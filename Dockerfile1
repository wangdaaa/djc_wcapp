FROM python:3.6
WORKDIR /root
COPY . /root/
RUN pip3 install --upgrade pip\
    &&pip3 install wheel\
    &&pip3 install --upgrade setuptools\
    &&pip3 install --default-timeout=100 -i https://pypi.tuna.tsinghua.edu.cn/simple -r /root/requirements.txt
RUN sed -i '36,37d'  /usr/local/lib/python3.6/site-packages/django/db/backends/mysql/base.py
CMD uwsgi --ini /root/djc_uwsgi1.ini
